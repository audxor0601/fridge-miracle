# 냉장고의기적 2.0 — 설계서 v0.1

작성일 2026-09-08 · 작성 홍명택 · 상태 **설계 확정 대기**

> 진행 원칙: ① 설계 먼저 ② 실제 데이터로 검증 ③ 한 번에 한 단계 ④ 막힌 지점은 본인 문장으로 기록

---

## 0. 1.0 진단 — 무엇이 있고, 무엇이 없는가

| 자산 | 실체 | 2.0에서 |
|---|---|---|
| 목업 HTML (313줄) | 화면 흐름 3개, 디자인 토큰 | 흐름·톤만 계승 |
| "Random Forest 88.4% / XGBoost MAE 4.12" | `Math.random()` 출력 | **삭제** |
| "Claude 실시간 레시피 생성" | 하드코딩 배열 + `setTimeout` | 공공 DB로 대체 |
| `urgency_score = 1/(남은일수+1)` | 실제 동작하는 공식 | **계승** |
| AI Studio ML 계획서 앱 | 규칙 기반 시뮬레이터 | 문서로만 참조 |
| 데이터 | 상수 4건, 새로고침 시 소멸 | Supabase 영속화 |

**진단**: 1.0은 설계서 검토용 프로토타입. 2.0은 "가짜를 진짜로 바꾸는 프로젝트"이고, 이 대비 자체가 포트폴리오의 서사가 된다.

<details>
<summary><b>표 읽는 법</b> — 세 칸이 각각 무엇인지 (설명용 주석)</summary>

한 문장 요약: **1.0에서 만든 것을 하나씩 열어보고, 겉으로 내세운 것과 실제 코드가 하는 일이 같은지 대조한 뒤, 2.0에서 살릴지 버릴지 판정한 표.**

| 열 | 의미 |
|---|---|
| **자산** | 1.0에서 만들어 둔 결과물 목록. 화면, 표시된 성능 지표, 데이터, 문서까지 빠짐없이 나열 |
| **실체** | 그것이 코드 수준에서 실제로 무엇이었는지. 겉면과 속을 분리해 적은 칸 |
| **2.0에서** | 그래서 이번에 어떻게 처리하는지 — 계승 / 삭제 / 대체 중 하나로 결론 |

**이 표를 먼저 만든 이유**: 재작성인지 개선인지 판단하려면 기존 것의 실체부터 알아야 했다. 정리해 보니 동작하는 자산은 유통기한 긴급도 공식과 화면 흐름 정도였고 성능 지표는 전부 연출이었다. 그래서 2.0의 목표를 '기능 추가'가 아니라 **표시된 것과 실제가 일치하는 서비스 만들기**로 잡았다.

</details>

<details>
<summary><b>"Random Forest 88.4% / XGBoost MAE 4.12 → <code>Math.random()</code> 출력 → 삭제"</b>가 무슨 뜻인가 (비전공자용 설명)</summary>

**용어부터**
- **Random Forest / XGBoost** — 머신러닝 알고리즘 이름. 앞은 분류(이 요리가 한식이냐 양식이냐), 뒤는 예측(영양 점수 몇 점이냐)에 쓴다.
- **88.4%** — 분류 정확도. 100번 중 88번을 맞혔다는 뜻.
- **MAE 4.12** — 평균 절대 오차. 예측한 점수가 실제와 평균 4.12점 차이났다는 뜻.

**그런데 1.0에서는**

이 숫자들이 나오려면 데이터를 모으고, 모델을 학습시키고, 시험 데이터로 채점하는 과정이 있어야 한다. 1.0에는 그 과정이 없었다. 화면의 숫자는 `Math.random()` — **난수 생성 함수**가 만들어낸 값이었다. 버튼을 누를 때마다 다른 숫자가 나오고, 새로고침하면 또 바뀐다. 옆에 뜨던 "ML 분석 중..." 로딩도 실제 계산이 아니라 1.2초 기다렸다가 다음 화면을 보여주는 연출(`setTimeout`)이었다.

비유하자면 **온도계를 달지 않고 온도계 그림에 숫자를 적어 넣은 것**에 가깝다. 화면은 그럴듯하지만 그 숫자가 가리키는 실제 측정은 없다.

**그게 잘못이었나**

당시 목적을 생각하면 아니다. 1.0은 학교 과제의 *설계서 검토용 프로토타입*이었고, "이런 화면과 이런 지표를 만들 계획"임을 보여주는 게 목적이었다. 목업에서 가짜 데이터를 쓰는 건 정상이다.

**그럼 왜 삭제하나**

용도가 달라지기 때문이다. 포트폴리오는 "내가 실제로 만든 것"을 증명하는 물건인데, 근거 없는 성능 수치가 하나라도 섞여 있으면 나머지 진짜 작업까지 의심받는다. 면접에서 "이 88.4%는 어떻게 나온 값이죠?"라는 질문 한 번이면 끝난다.

그래서 2.0에서는 **지어낸 숫자를 지우고, 실제로 측정한 값만 남긴다.** 수집한 레시피 건수, 재료 파싱 정확도, 추천 응답 시간 — 전부 직접 세고 재서 나온 숫자다. 없는 것을 있는 척하는 대신, 실제로 있는 것을 정확히 보여주는 쪽을 택했다.

</details>

---

## 1. 2.0 목표와 범위

**한 줄 정의** — 내 냉장고 재고를 등록해두면, 유통기한이 급한 재료부터 소비하도록 실제 레시피를 추천하고, 상하기 전에 알려주는 웹 서비스.

### 범위에 있는 것 (MVP)
- 이메일 회원가입 / 로그인
- 재고 등록·수정·삭제 (재료명, 수량, 구매일, 유통기한)
- 유통기한 D-day 대시보드 (인앱 배지)
- 보유 재료 기반 레시피 추천 (규칙 기반 + 유통기한 가중치)
- 매일 아침 이메일 다이제스트 ("오늘 3개 재료가 만료됩니다")

### 범위 밖 (명시적 제외)
- 머신러닝 모델 학습 — 실사용 데이터가 쌓인 뒤 2단계
- 웹 푸시 알림, 소셜 로그인, 바코드 스캔, 가족 공유
- 이미지 인식 재료 등록

경계를 문서에 박아두는 이유: 범위가 흐려지면 완성이 안 되고, 미완성 포트폴리오는 감점이다.

---

## 2. 기술 스택

| 층 | 선택 | 근거 |
|---|---|---|
| 프론트 | React 19 + Vite + TypeScript | 채용공고 최빈 조합, 코드 설명이 쉬움 |
| 상태/데이터 | TanStack Query + Supabase JS | 서버 상태와 UI 상태 분리 |
| 스타일 | Tailwind CSS v4 | 1.0의 Natural Tones 팔레트 이식 |
| 백엔드 | Supabase (Postgres + Auth + RLS + Edge Functions) | SQL 설계를 보여줄 수 있음 |
| 배치 | Supabase Edge Function + pg_cron | 서버리스 스케줄 작업 경험 |
| 메일 | Resend (Edge Function에서 호출) | 무료 티어, 도메인 없이 발신 가능 |
| 수집 | Python 스크립트 (일회성 ETL) | 판다스로 정제·검증 리포트 |
| 배포 | Vercel (프론트) + Supabase (백엔드) | GitHub Pages는 SPA 라우팅·환경변수에서 불리 |

> 참고: Supabase anon key는 공개되어도 안전하다. 데이터를 지키는 것은 키가 아니라 RLS 정책이다. 이 점을 README에 쓰면 보안 이해도가 드러난다.

---

## 3. 데이터 모델

### 3.1 ERD 요약

```
auth.users ─1:1─ profiles
profiles ─1:N─ storage_items ─N:1─ ingredients
recipes ─1:N─ recipe_steps
recipes ─1:N─ recipe_ingredients ─N:1─ ingredients
profiles ─1:N─ notification_log
```

핵심은 `ingredients`가 **재고와 레시피의 공통 사전** 역할을 한다는 것. 매칭이 문자열 비교가 아니라 FK 조인으로 이루어진다.

### 3.2 테이블 명세

**profiles** — 사용자 설정
| 컬럼 | 타입 | 비고 |
|---|---|---|
| id | uuid PK | `auth.users.id` 참조 |
| nickname | text | |
| household_size | int | 기본 1 (1인 가구 타겟) |
| notify_email | bool | 다이제스트 수신 여부 |
| notify_hour | int | 발송 시각(KST), 기본 8 |
| created_at | timestamptz | |

**ingredients** — 재료 마스터 사전
| 컬럼 | 타입 | 비고 |
|---|---|---|
| id | bigint PK | |
| name | text UNIQUE | 정규화된 표준명 ("대파") |
| aliases | text[] | ["파", "쪽파", "실파"] |
| category | text | 육류/채소/유제품/양념/곡물 |
| default_shelf_life_days | int | 유통기한 미입력 시 추정용 |
| is_seasoning | bool | 양념은 매칭 점수에서 제외 |

**storage_items** — 내 냉장고 재고
| 컬럼 | 타입 | 비고 |
|---|---|---|
| id | bigint PK | |
| user_id | uuid FK | |
| ingredient_id | bigint FK | |
| qty | numeric | |
| unit | text | g / ml / 개 / 큰술 |
| purchased_at | date | |
| expires_on | date | NULL이면 구매일 + 기본 소비기한 |
| status | text | active / consumed / discarded |

> `discarded`를 남기는 이유: "버린 재료" 통계가 나중에 ML 단계의 학습 라벨이 된다.

**recipes** — 공공 API 원본 (COOKRCP01)
| 컬럼 | 타입 | 원본 필드 |
|---|---|---|
| id | bigint PK | |
| source_id | text UNIQUE | RCP_SEQ |
| name | text | RCP_NM |
| way | text | RCP_WAY2 (굽기/끓이기…) |
| category | text | RCP_PAT2 (반찬/국·찌개…) |
| kcal, carb, protein, fat, sodium | numeric | INFO_ENG / CAR / PRO / FAT / NA |
| image_url | text | ATT_FILE_NO_MK |
| hashtags | text | HASH_TAG |
| raw_parts | text | RCP_PARTS_DTLS (파싱 전 원문 보존) |

**recipe_steps** — MANUAL01~20 정규화 (recipe_id, step_no, text, image_url)

**recipe_ingredients** — 파싱 결과
| 컬럼 | 타입 | 비고 |
|---|---|---|
| recipe_id | bigint FK | |
| ingredient_id | bigint FK | 매칭 실패 시 NULL |
| raw_text | text | "돼지고기 100g" 원문 |
| qty | numeric | 파싱 성공 시 |
| unit | text | |
| is_essential | bool | 양념 아니면 true |

**notification_log** — 발송 이력 (중복 발송 방지 + 재현 가능한 근거)

### 3.3 RLS 정책 (요지)
- `profiles`, `storage_items`, `notification_log` : `auth.uid() = user_id` 인 행만 select/insert/update/delete
- `ingredients`, `recipes`, `recipe_steps`, `recipe_ingredients` : 누구나 select, 쓰기는 service_role만

---

## 4. 추천 엔진 명세 (규칙 기반)

가짜 ML을 걷어낸 자리에 들어갈 **설명 가능한 공식**.

### 4.1 긴급도
```
urgency(i) = 1 / (남은일수 + 1)      # 1.0에서 계승
남은일수 < 0  → 만료, 추천 대상에서 제외하고 '폐기 후보'로 분리
남은일수 = 0  → urgency 1.00 (D-day)
남은일수 = 3  → urgency 0.25
```

### 4.2 레시피 점수
레시피 r의 필수 재료 집합을 E(r), 그중 내가 가진 것을 H(r)이라 하면

```
coverage(r) = |H(r)| / |E(r)|                        # 0 ~ 1
urgency_sum(r) = Σ_{i ∈ H(r)} urgency(i)
urgency_norm(r) = min(urgency_sum(r) / |E(r)|, 1)    # 0 ~ 1

score(r) = 100 × (0.65 × coverage + 0.35 × urgency_norm)
```

**하드 필터**: `coverage < 0.5` 인 레시피는 목록에서 제외 (재료 절반도 없는 요리를 추천하면 신뢰를 잃는다)

**가중치 근거**: 재료가 있어야 요리가 성립하므로 coverage가 주(0.65), 유통기한은 순서를 바꾸는 조정 변수(0.35). 이 비율은 실제 재고로 돌려보고 조정한다 — 조정 과정과 근거를 README에 남긴다.

### 4.3 화면에 보여줄 근거
"매칭률 75%" 한 줄로 끝내지 않는다. `삼겹살(D-1), 대파(D-day) 소비 · 양파 부족` 처럼 **왜 이 순서인지**를 그대로 노출한다. 설명 가능성이 규칙 기반을 선택한 이유이므로, 그 이점을 화면에서 보여줘야 한다.

---

## 5. 유통기한 알림 파이프라인

```
pg_cron (매시 정각)
  └─ Edge Function: digest
       ├─ notify_email = true 이고 notify_hour = 현재 KST 시각인 프로필 조회
       ├─ 각 사용자의 storage_items 중 expires_on ≤ today + 3 인 행 집계
       ├─ 오늘 이미 보낸 기록(notification_log)이 있으면 건너뜀
       ├─ 추천 레시피 상위 3개 계산
       └─ Resend로 메일 발송 → notification_log 기록
```

인앱 배지는 별도 배치 없이 재고 조회 시 `expires_on`으로 즉시 계산한다.

**설계상 주의**: 시간대. Supabase는 UTC, 사용자는 KST(UTC+9). 매시 실행 + 사용자별 발송 시각 비교 방식으로 처리한다.

---

## 6. 외부 데이터 수집 (실제 데이터 검증의 핵심)

**소스**: 식품의약품안전처 「조리식품의 레시피 DB」(COOKRCP01), 무료·이용허락범위 제한 없음, JSON 제공

**파이프라인**
1. 공공데이터포털에서 키 발급 → 전체 페이지 순회 수집 → 원본 JSON 보관
2. `recipes` / `recipe_steps` 적재 (MANUAL01~20 중 빈 칸 제거)
3. `RCP_PARTS_DTLS` 파싱 → `recipe_ingredients`

**최대 난관은 3번이다.** 원문이 `"돼지고기 100g, 양파 1/2개, 진간장 2큰술"` 같은 자유 텍스트라 구분자도 단위 표기도 일정하지 않다. 계획:
- 구분자 후보(`,` `·` 개행)로 1차 분해
- 정규식으로 `수량 + 단위` 추출 (`1/2`, `2큰술`, `100g`, `약간`)
- 재료명은 `ingredients.aliases` 사전으로 표준명 매핑
- 매칭 실패 항목은 NULL로 남기고 **실패 목록을 파일로 출력** — 사전을 채우는 근거로 사용

**검증 기준 (이 단계의 완료 정의)**
- 수집 건수와 결측률 리포트 출력
- 무작위 30건을 원문과 눈으로 대조해 파싱 정확도 측정, **85% 이상**이면 통과
- 미달 시 사전 보강 후 재측정 — 이 반복 과정을 기록으로 남긴다

---

## 7. 화면 구성

| 경로 | 화면 | 내용 |
|---|---|---|
| `/` | 랜딩 | 서비스 소개, 로그인/가입 |
| `/fridge` | 내 냉장고 | 재고 목록(D-day 정렬), 추가/수정/삭제, 만료 임박 배지 |
| `/recipes` | 추천 | 점수순 레시피 카드 + 추천 근거 |
| `/recipes/:id` | 상세 | 재료(보유/부족 표시), 조리 순서, 영양 정보 |
| `/settings` | 설정 | 알림 수신 여부·시각, 가구원 수 |
| `/about` | 개발 노트 | 수집 건수·파싱 정확도·응답 시간 등 **실측치** (1.0의 가짜 ML 지표 자리) |

---

## 8. 단계별 로드맵

각 단계는 **완료 정의(DoD)를 통과해야** 다음으로 넘어간다. 한 번에 한 단계.

| # | 단계 | 완료 정의 |
|---|---|---|
| 0 | 저장소·환경 | GitHub 저장소 생성, Vite+TS 초기 화면이 로컬에서 뜬다. 맥/윈도우 양쪽에서 clone 후 실행 확인 |
| 1 | Supabase 프로젝트 + 스키마 | 위 DDL 적용, RLS 켜짐. 대시보드에서 테이블 확인 |
| 2 | 레시피 수집 | 전체 건수 적재, 결측률 리포트 출력 |
| 3 | 재료 파싱 | 무작위 30건 대조 정확도 85% 이상 |
| 4 | 인증 | 가입→로그인→로그아웃 왕복, 새로고침해도 세션 유지 |
| 5 | 재고 CRUD | 등록/수정/삭제 동작. **다른 계정으로 로그인하면 안 보이는 것까지 확인** (RLS 검증) |
| 6 | D-day 대시보드 | 마스터 냉장고의 진짜 재료 10건 이상 등록해 정렬·배지 확인 |
| 7 | 추천 엔진 | 실제 재고로 추천 결과가 납득 가능한지 검토, 가중치 조정 및 근거 기록 |
| 8 | 이메일 다이제스트 | 본인 메일로 실제 수신, 중복 발송 없음 |
| 9 | 배포 + README | 공개 URL 접속, README의 "막혔던 문제와 해결" 작성 |

---

## 9. 막힐 것으로 예상되는 지점

미리 표시해 둔다. 실제로 부딪히면 **마스터 본인 문장으로** 아래에 채운다 — 이 항목이 면접에서 가장 많이 질문받는 부분이다.

1. **재료명 정규화** — "대파 / 파 / 쪽파"를 어디까지 같다고 볼 것인가. 사전을 어떻게 만들 것인가
2. **RLS 정책 디버깅** — 정책이 틀리면 에러가 아니라 "빈 배열"이 온다. 원인 찾기가 까다롭다
3. **Edge Function 시간대** — UTC/KST 혼선으로 발송 시각이 9시간 어긋나는 문제
4. **추천 가중치** — 0.65/0.35가 맞는 값인지 판단할 기준
5. **공공 API 응답 형식** — 페이지네이션 한도, 결측 필드 처리

---

## 10. 다음 행동

**Step 0부터 시작한다.** 저장소 이름과 서비스 도메인 표기(영문명)를 정한 뒤, Vite 프로젝트를 세우고 맥·윈도우 양쪽에서 동일하게 뜨는 것까지 확인한다.

---

### 출처
- [식품의약품안전처_조리식품의 레시피 DB (공공데이터포털)](https://www.data.go.kr/data/15060073/openapi.do)
- [조리식품의 레시피 DB API 명세 (식품안전나라)](https://www.foodsafetykorea.go.kr/api/openApiInfo.do?menu_grp=MENU_GRP31&menu_no=661&show_cnt=10&start_idx=1&svc_no=COOKRCP01)
